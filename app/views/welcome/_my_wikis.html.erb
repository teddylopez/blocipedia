<div class="col-md-6">
  <% if @my_wikis.empty? %>
    <h2>You don't have any notes.</h2>
    <div class="start-btn"><%= link_to 'Start writing!', new_wiki_path, class: 'btn btn-success' %></div>
  <% else %>
    <h2>Welcome back. Here are your recent notes:</h2>
    <ul>
      <% @my_wikis.each do |wiki| %>
        <li><%= link_to wiki.title, wiki %></li>
      <% end %>
    </ul>
  <% end %>
</div>
