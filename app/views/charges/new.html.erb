<div class="container">
  <%= form_tag charges_path do %>
    <h4>Click the button!</h4>
    <script class="stripe-button" data-amount="<%= @stripe_btn_data[:amount] %>" data-description="<%= @stripe_btn_data[:description] %>" data-key="<%= @stripe_btn_data[:key] %>" src="https://checkout.stripe.com/checkout.js"></script>
  <% end %>
</div>
